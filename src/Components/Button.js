import React from 'react'
const Button = () => {
  return (
    <>
      {/* window.scrollTo({
    top: document.documentElement.scrollHeight,
    behavior: "smooth",
  }); */}
    </>
  )
}

export default Button
